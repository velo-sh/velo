[meta]
package = "tomli"
category = "library"
tier = 3

description = "tomli compatibility test"

[compat]
sys_path_strategy = "source"
source = "git"
git_repo = "https://github.com/hukkin/tomli.git"
git_ref = "2.0.1"
test_path = "tests"
test_dependencies = ["pytest"]
pytest_args = ["-x"]
timeout = 300
known_failures = []
