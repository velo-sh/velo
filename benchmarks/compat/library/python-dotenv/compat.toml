[meta]
package = "python-dotenv"
category = "library"
tier = 0

description = "python-dotenv compatibility test"

[compat]
sys_path_strategy = "source"
source = "git"
git_repo = "https://github.com/theskumar/python-dotenv.git"
git_ref = "v1.0.0"
test_path = "tests"
test_dependencies = ["pytest", "sh", "click"]
pytest_args = ["-x"]
timeout = 300
known_failures = []
