[meta]
package = "wheel"
category = "library"
tier = 3

description = "wheel compatibility test"

[compat]
sys_path_strategy = "source"
source = "git"
git_repo = "https://github.com/pypa/wheel.git"
git_ref = "0.41.3"
test_path = "tests"
test_dependencies = ["pytest"]
pytest_args = ["-x"]
timeout = 300
known_failures = []
