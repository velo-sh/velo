[meta]
package = "s3transfer"
category = "library"
tier = 0

description = "s3transfer compatibility test"

[compat]
sys_path_strategy = "source"
source = "git"
git_repo = "https://github.com/boto/s3transfer.git"
git_ref = "0.7.0"
test_path = "tests"
test_dependencies = ["pytest"]
pytest_args = ["-x"]
timeout = 300
known_failures = []
