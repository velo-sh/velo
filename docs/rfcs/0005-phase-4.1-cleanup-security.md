# RFC-0005: Phase 4.1 Cleanup & Security

> **Status**: `Draft`  
> **Author**: Architect  
> **Created**: 2026-01-02  
> **Target Release**: Velo v0.4.1  
> **Branch**: `phase-4.1/cleanup-security`

---

## 1. Executive Summary

### 1.1 Background

Phase 4.0 delivered `velo analyze` for automatic import bottleneck detection. However, two issues remain:

1. **Legacy Hardcoding**: `src/serve/framework.rs` still contains hardcoded framework detection
2. **Security Risk**: `velo analyze` executes user code without warning

### 1.2 Goal

Clean up legacy patterns and add safety mechanisms:

```bash
# Deprecated warning when using hardcoded detection
warning: Framework enum is deprecated since v0.4.1

# Safe analysis without code execution
velo analyze --dry-run

# Skip confirmation for CI/scripts
velo analyze --yes
```

---

## 2. Technical Design

### 2.1 DELETE framework.rs (Not Deprecate)

**Decision**: Delete entire file, not deprecate.

**Reason**: `velo analyze --suggest-preload` replaces hardcoded `get_preload_modules()`.

**Files to Modify**:
- DELETE: `src/serve/framework.rs`
- MODIFY: `src/serve/runner.rs` (remove framework import)
- MODIFY: `src/serve/mod.rs` (remove framework export)

---

### 2.2 New CLI Flags

```rust
// src/cmd/analyze.rs
#[arg(long)]
pub dry_run: bool,   // Don't execute code

#[arg(long, short = 'y')]
pub yes: bool,       // Skip confirmation
```

---

### 2.3 Consent Prompt

```rust
if !args.dry_run && !args.yes && is_interactive() {
    eprintln!("⚠️  WARNING: This will execute your Python code.");
    eprint!("Continue? [y/N]: ");
    if !read_confirmation() { return Ok(()); }
}
```

---

## 3. Phase 4.1 Scope

| Task | Status | 工作量 |
|------|--------|--------|
| DELETE framework.rs | **MUST** | 1 day |
| --dry-run flag | **MUST** | 0.5 day |
| --yes flag | **MUST** | 0.5 day |
| Consent prompt | **MUST** | 0.5 day |
| Split analyze.rs | OPTIONAL | 2 days |
| Sandbox | DEFER to 5.x | - |

**Total**: 2.5 days (MUST only)

---

## 4. Implementation Plan

### Day 1: DELETE framework.rs

1. Remove `src/serve/framework.rs`
2. Update `src/serve/mod.rs`
3. Update `src/serve/runner.rs`
4. Remove framework tests

### Day 2: Security Flags

1. Add `--dry-run` flag
2. Add `--yes` flag  
3. Implement consent prompt
4. Integration tests

---

## 5. Success Metrics

| Metric | Target |
|--------|--------|
| Deprecated warnings | 100% coverage |
| --dry-run | No code execution |
| Default behavior | Prompts for confirmation |

---

## 6. Verification Plan

```bash
# Test deprecated warning
cargo test 2>&1 | grep -i deprecated

# Test --dry-run
velo analyze --dry-run main.py
# Should NOT execute main.py

# Test --yes
echo "y" | velo analyze main.py
# Should proceed without prompt
```

---

**Document End**
