repos:
  - repo: local
    hooks:
      - id: sync-constants
        name: sync-constants (TOML→Python)
        entry: uv run python scripts/sync-constants.py --check
        language: system
        files: ^(config/constants\.toml|velo_zygote/constants\.py|scripts/sync-constants\.py)$
        pass_filenames: false

      - id: mypy
        name: mypy (zygote)
        entry: uv run mypy
        language: system
        files: ^velo_zygote/
        types: [python]
        pass_filenames: true

  - repo: local
    hooks:
      - id: cargo-fmt
        name: cargo fmt
        description: Format Rust code
        entry: cargo fmt --all
        language: system
        files: \.rs$
        pass_filenames: false

      - id: cargo-clippy
        name: cargo clippy
        description: Lint and fix Rust code
        entry: cargo clippy --fix --allow-dirty --workspace --all-targets -- -D warnings
        language: system
        files: \.rs$
        pass_filenames: false
