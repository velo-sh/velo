repos:
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.8.0
    hooks:
      - id: mypy
        name: mypy (zygote)
        files: ^velo_zygote/
        additional_dependencies: [types-requests, types-psutil]
